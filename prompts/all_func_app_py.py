# app.py
def main_app_flow():
    """
    该脚本是整个AI文档生成器的用户界面（UI）层。它使用Streamlit库构建一个Web应用，允许用户通过一个简单的文本框输入自然语言指令，并接收生成的Word文档。该文件本身不包含任何核心的文档生成或AI解析逻辑，而是作为前端，调用后端服务（`src.app_logic.generate_document_from_command`）来完成所有实际工作，并负责将结果和处理过程清晰地展示给用户。

    Args (隐式):
        user_command (str): 用户在Web界面的文本区域中输入的自然语言指令。这是整个流程的起点。

    Returns/Output (用户界面):
        - 一个可供下载的 `.docx` 文件。
        - 实时状态更新（如加载提示）。
        - 成功或失败的提示信息。
        - 用于调试和透明化的可折叠区域，显示AI处理日志和最终生成的JSON数据。
        - 详细的错误堆栈信息（在发生异常时）。
    """